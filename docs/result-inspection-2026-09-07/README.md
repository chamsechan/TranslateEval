# 评测结果核对验证

2026-09-07 使用 Playwright Chromium 验证构建后的前端及真实 HTTP API。测试使用 `/tmp` 下的独立 SQLite 数据库、导入目录及服务端口，不运行 Worker，不调用模型，不访问项目实际评测数据库。

14 条固定样本分布：泰语 8 条，越南语 4 条，印尼语 2 条。7 条具有有效评分；其余覆盖评分失败、取消、等待、评分中、缺少评分项及缺少预测记录。失败项附带旧评分，确认旧分不会参与统计或分数筛选。

- 阈值为 8 时，正确数 4 / 总数 14，加权平均准确率 28.6%；泰语为 2 / 8 = 25%，完全未评分的印尼语为 0%。
- 阈值改为 9 时，准确率为 2 / 14 = 14.3%，保留原先的语种与分数筛选。
- 通过语种、查看明细及未评分数量入口定位逐句结果；未评分记录可展开核对源文、参考译文、候选译文和错误原因。
- 验证 `=`、`<`、`≤`、`>`、`≥` 五种分数条件，所有评分状态，以及语种、状态与分数组合筛选、清除筛选、空结果提示。得分为 0 的有效样本与未评分区分处理。
- 验证 1440px、1024px 布局及哈希详情链接刷新；无页面横向溢出，表格使用自身横向滚动。
- 18 项浏览器检查通过，无 JavaScript 运行时错误或失败的 API 请求。浏览器默认请求不存在的 `favicon.ico` 所产生的既有 404 单独记录，不影响应用。

详细断言和 API 请求记录见 `browser-verification.json`，统计响应见 `summary-threshold-8.json`；截图分别展示完整页面、泰语小于 8 分筛选，以及泰语未评分明细。
